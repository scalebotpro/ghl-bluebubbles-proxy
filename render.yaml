services:
  - type: web
    name: ghl-bluebubbles-proxy
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GHL_PRIVATE_TOKEN
        sync: false
      - key: BLUEBUBBLES_URL
        sync: false
      - key: PORT
        value: 10000
